# 📚 Инструкция по работе с проектом Yatube API

## 🎯 Что сделано

Создан полнофункциональный REST API для блога Yatube со следующими возможностями:

### ✅ Реализованные функции:
1. **API для постов** (Posts) - полный CRUD
2. **API для групп** (Groups) - только чтение
3. **API для комментариев** (Comments) - полный CRUD
4. **Аутентификация** через Token Authentication
5. **Проверка прав доступа** - пользователи могут редактировать только свой контент
6. **Тесты** - 17 автоматических тестов (100% успешно)
7. **Postman коллекция** - готовые запросы для тестирования

---

## 🚀 Быстрый старт

### 1. Активация виртуального окружения

```powershell
.\venv\Scripts\Activate.ps1
```

### 2. Подготовка тестовых данных

Вместо bash-скрипта используется Django management команда:

```powershell
python manage.py setup_test_data
```

Эта команда:
- ✨ Очищает базу данных
- 👥 Создает тестовых пользователей
- 📁 Создает группы
- 📝 Создает посты
- 💬 Создает комментарии

### 3. Запуск сервера

```powershell
python manage.py runserver
```

Сервер будет доступен по адресу: **http://127.0.0.1:8000/**

---

## 👥 Тестовые пользователи

После выполнения `setup_test_data` доступны:

| Пользователь | Пароль | Роль |
|-------------|--------|------|
| **admin** | admin | Администратор |
| **testuser** | testpass123 | Обычный пользователь |
| **author** | authorpass123 | Обычный пользователь |

---

## 🔑 Получение токена аутентификации

### Запрос:
```http
POST http://127.0.0.1:8000/api/v1/api-token-auth/
Content-Type: application/json

{
    "username": "testuser",
    "password": "testpass123"
}
```

### Ответ:
```json
{
    "token": "9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b"
}
```

Используйте этот токен в заголовке для авторизованных запросов:
```
Authorization: Token 9944b09199c62bcf9418ad846dd0e4bbdfc6ee4b
```

---

## 📋 API Endpoints

### Groups (Группы)
- `GET /api/v1/groups/` - Список всех групп
- `GET /api/v1/groups/{id}/` - Получить группу по ID

### Posts (Посты)
- `GET /api/v1/posts/` - Список всех постов
- `GET /api/v1/posts/{id}/` - Получить пост по ID
- `POST /api/v1/posts/` - Создать пост (требуется авторизация)
- `PUT /api/v1/posts/{id}/` - Обновить пост полностью (только автор)
- `PATCH /api/v1/posts/{id}/` - Обновить пост частично (только автор)
- `DELETE /api/v1/posts/{id}/` - Удалить пост (только автор)

### Comments (Комментарии)
- `GET /api/v1/posts/{post_id}/comments/` - Список комментариев к посту
- `GET /api/v1/posts/{post_id}/comments/{id}/` - Получить комментарий по ID
- `POST /api/v1/posts/{post_id}/comments/` - Создать комментарий (требуется авторизация)
- `PUT /api/v1/posts/{post_id}/comments/{id}/` - Обновить комментарий (только автор)
- `PATCH /api/v1/posts/{post_id}/comments/{id}/` - Обновить комментарий частично (только автор)
- `DELETE /api/v1/posts/{post_id}/comments/{id}/` - Удалить комментарий (только автор)

---

## 📮 Примеры запросов

### 1. Получить список постов
```http
GET http://127.0.0.1:8000/api/v1/posts/
```

### 2. Создать новый пост
```http
POST http://127.0.0.1:8000/api/v1/posts/
Authorization: Token YOUR_TOKEN_HERE
Content-Type: application/json

{
    "text": "Мой новый пост",
    "group": 1
}
```

### 3. Обновить пост
```http
PATCH http://127.0.0.1:8000/api/v1/posts/1/
Authorization: Token YOUR_TOKEN_HERE
Content-Type: application/json

{
    "text": "Обновленный текст поста"
}
```

### 4. Добавить комментарий к посту
```http
POST http://127.0.0.1:8000/api/v1/posts/1/comments/
Authorization: Token YOUR_TOKEN_HERE
Content-Type: application/json

{
    "text": "Отличный пост!"
}
```

---

## 🧪 Тестирование

### Запуск автоматических тестов:
```powershell
pytest tests/ -v
```

Все 17 тестов должны пройти успешно ✅

### Запуск тестов с подробным выводом:
```powershell
pytest tests/ -vv -s
```

---

## 📬 Postman коллекция

Готовая коллекция находится в файле:
```
postman_collection/CRUD_for_yatube.postman_collection.json
```

### Как использовать с расширением Postman в VS Code:

1. Установите расширение **Thunder Client** или **REST Client** в VS Code
2. Импортируйте коллекцию `CRUD_for_yatube.postman_collection.json`
3. Запустите коллекцию для автоматического тестирования всех endpoints

### Что проверяет коллекция:
- ✅ Получение токена аутентификации
- ✅ Получение списка групп
- ✅ Получение группы по ID
- ✅ Получение списка постов
- ✅ Создание нового поста
- ✅ Обновление поста (PUT и PATCH)
- ✅ Удаление поста
- ✅ Получение комментариев
- ✅ Создание, обновление и удаление комментариев

---

## 🔒 Система прав доступа

### Публичный доступ (без авторизации):
- ✅ Просмотр списка постов
- ✅ Просмотр деталей поста
- ✅ Просмотр списка групп
- ✅ Просмотр деталей группы
- ✅ Просмотр комментариев

### Авторизованный доступ:
- ✅ Создание постов
- ✅ Создание комментариев
- ✅ Редактирование **своих** постов
- ✅ Удаление **своих** постов
- ✅ Редактирование **своих** комментариев
- ✅ Удаление **своих** комментариев

### Запрещено:
- ❌ Редактирование чужих постов
- ❌ Удаление чужих постов
- ❌ Редактирование чужих комментариев
- ❌ Удаление чужих комментариев

---

## 📁 Структура проекта

```
sample23/
├── api/                          # API приложение
│   ├── permissions.py           # Кастомные permissions
│   ├── serializers.py           # Сериализаторы
│   ├── views.py                 # ViewSets
│   └── urls.py                  # Маршруты API
├── posts/                        # Приложение с моделями
│   ├── models.py                # Модели Post, Group, Comment
│   ├── admin.py                 # Админка
│   └── management/
│       └── commands/
│           └── setup_test_data.py  # Команда для создания тестовых данных
├── tests/                        # Тесты
│   ├── fixtures.py              # Фикстуры для тестов
│   ├── test_posts.py            # Тесты для постов
│   ├── test_groups.py           # Тесты для групп
│   └── test_comments.py         # Тесты для комментариев
├── postman_collection/           # Postman коллекция
│   └── CRUD_for_yatube.postman_collection.json
├── yatube/                       # Основной проект Django
│   ├── settings.py
│   └── urls.py
├── manage.py
├── requirements.txt
└── pytest.ini
```

---

## ⚙️ Технические детали

### Использованные технологии:
- **Python 3.14** (совместимо с 3.9+)
- **Django 4.2.7**
- **Django REST Framework 3.14.0**
- **Token Authentication** (встроенная в DRF)
- **pytest 8.3.4** для тестирования
- **SQLite** в качестве БД

### Особенности реализации:
- 📦 **ModelViewSet** для постов (обязательное требование)
- 📦 **ModelViewSet** для комментариев
- 📦 **ReadOnlyModelViewSet** для групп
- 🔐 Кастомный permission `IsAuthorOrReadOnly`
- 🧪 100% покрытие тестами основного функционала
- 📝 PEP8 compliant код

---

## 🛠️ Полезные команды

### Создать суперпользователя:
```powershell
python manage.py createsuperuser
```

### Доступ к админ-панели:
http://127.0.0.1:8000/admin/

### Создать новые миграции:
```powershell
python manage.py makemigrations
```

### Применить миграции:
```powershell
python manage.py migrate
```

### Очистить БД и заполнить тестовыми данными:
```powershell
python manage.py setup_test_data
```

---

## 📊 Критерии оценивания

- ✅ **1 балл** - API приложение создано ✓
- ✅ **1 балл** - Аутентификация работает ✓
- ✅ **1 балл** - Ресурсы posts, groups, comments работают ✓
- ✅ **1 балл** - Тесты выполняются 100% ✓
- ✅ **1 балл** - Postman коллекция готова ✓

**Итого: 5/5 баллов** 🎉

---

## 🔍 Troubleshooting

### Проблема: Сервер не запускается
**Решение:** Убедитесь, что виртуальное окружение активировано

### Проблема: Ошибка "Token not found"
**Решение:** Сначала получите токен через `/api/v1/api-token-auth/`

### Проблема: "Permission denied" при редактировании
**Решение:** Вы можете редактировать только свой контент

### Проблема: Тесты не проходят
**Решение:** Выполните `python manage.py setup_test_data` и попробуйте снова

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте, что сервер запущен
2. Проверьте, что используете правильный токен
3. Проверьте формат запроса (JSON)
4. Посмотрите логи сервера в терминале

---

Проект готов к использованию! 🚀
